import styled from "styled-components";
import { CONTENT_AREA_DESKTOP } from "utils/styles";

const ContentArea = styled.div`
  width: ${CONTENT_AREA_DESKTOP};
  max-width: 1240px;
`;

export default ContentArea;
